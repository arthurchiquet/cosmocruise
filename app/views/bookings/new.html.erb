<div class="container">
  <h1><%= @trip.title %></h1>
  <h3>Book a reservation</h3>
  <hr>
  <%= render "form", list: @booking %>
  <hr>
  <%= link_to "Cancel", trip_path(@trip), class: 'btn btn-light' %>
</div>
